# Faz 1 eğitim pipeline'ı için Python bağımlılıkları.
# Colab'da tensorflow zaten kuruludur; ek paket gerekmez (tfds KULLANILMIYOR).
# Yerelde kurmak için: pip install -r requirements.txt
tensorflow>=2.13
numpy
pandas
pillow
